package com.example.foodtracker.domain
/*
import androidx.camera.core.ImageProxy
import com.example.foodtracker.utils.centerCrop

class NutritionImageAnalyzer(
    private val classifier: ImageAnalyzer,
) {
    // Change 320
    private fun analyze(image: ImageProxy){
        val rotationDegrees = image.imageInfo.rotationDegrees
        val bitmap = image
            .toBitmap()
            .centerCrop(320, 320)

        val result = classifier.classify(bitmap, rotationDegrees)
        image.close()
    }
}*/